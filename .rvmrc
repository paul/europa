
if [[ -d "${rvm_path:-$HOME/.rvm}/environments" \
  && -s "${rvm_path:-$HOME/.rvm}/environments/macruby-0.8" ]] ; then
  \. "${rvm_path:-$HOME/.rvm}/environments/macruby-0.8"
else
  rvm --create  "macruby-0.8"
fi
